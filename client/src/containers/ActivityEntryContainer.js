import React from "react";
import {connect} from "react-redux";

function mapDispatchToProps(dispatch){
  return function(dispatch){

  }
}

export default connect(null, mapDispatchToProps)(ActivityEntry);
